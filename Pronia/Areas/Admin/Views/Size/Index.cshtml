@model List<Size>
@{
    ViewData["Title"] = "Index page";
}


<div class="main-panel">
    <div>
        <a class="btn btn-success" asp-area="Admin" asp-controller="Size" asp-action="CreateSize">Create a new Size</a>
    </div>
    <div class="table-responsive pt-3">
        <table class="table table-dark">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Name
                    </th>
                    <th>
                        Settings
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (Size size in Model)
                {
                    <tr>
                        <td>
                            @size.Id
                        </td>
                        <td>
                            @size.Name
                        </td>
                        <td  class="d-flex">
                                <div>
                                    <a asp-area="Admin" asp-controller="Size" asp-action="Edit" asp-route-id="@size.Id" class="btn btn-warning"><i class="mdi mdi-border-color"></i></a>
                                </div>
                                <div>
                                    <a class="btn btn-danger" asp-area="Admin" asp-controller="Size" asp-action="Delete" asp-route-id="@size.Id"><i class="mdi mdi-delete"></i></a>
                                </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
